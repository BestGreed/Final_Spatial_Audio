# 验证范围

构建使用 `-Wall -Wextra -Werror`。build.ps1 自动运行 PCM 结构校验、Resolver 与资源测试；这些测试不写系统音频。

需要交互式 Windows 桌面的测试（手动执行）：

- `native/bin/popup-test.exe`：键盘导航、禁用项、子菜单延迟、鼠标过渡与点击。
- `native/bin/material-test.exe`：测试程序自己的黑白背景变化下，两级菜单真实合成透明度。
- `native/bin/profile-test.exe <空测试目录>`：读取当前设备，在隔离目录验证默认配置、实际快照捕获与损坏文件保护，不调用音频写入。
- `native/bin/FinalSpatialAudio.exe --check`：初始化或检查配置，不切换音频。
- `--smoke`：创建托盘并读取状态约两秒；同实例已运行时会直接退出，不能据此认定执行了独立启动测试。

桌面测试会短暂显示窗口，部分测试移动鼠标后恢复。它们不应在无交互桌面环境中作为强制 CI 测试。

音频切换和播放确认需要实际设备。自动测试不能证明每个驱动、声道布置或编码器都能正常播放，也不能代替逐扬声器和游戏规则测试。
